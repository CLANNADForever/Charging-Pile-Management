# NCS 充电桩平台 · 使用说明（开发/运行/扩展）

> 北理工课设：Qt6(C 车主端) + C++ 后端 + 模拟器。会越来越"多端"（未来 C 端/管理端/后端/模拟器/AI），
> 本文是统一约定与速查。需求与产品见 `docs/ncs-srs.md`。

## 0. 目录结构（端）
```
src/common         共享：entities / money(分) / billing(half-up) / phone(校验)  [Qt 类型]
src/client_user    C 车主端库 ncs_user_ui(services+views, 无 main) + 可执行 ncs_user
src/backend_server 后端库 ncs_server_lib + 可执行 ncs_server  (HTTP + 模拟器TCP + SQLite)
src/simulator      模拟器 ncs_simulator（纯 C++，无 Qt）
tests/             ctest：ncs_offscreen / ncs_backend / ncs_e2e
third_party/       vendored：httplib.h、nlohmann/json.hpp
```

## 1. 构建（132 VM）
依赖已装：Qt6 6.2.4(Widgets/Network/WebEngine/Charts)、系统 SQLite、第三方头 vendored。
**必须带 Vulkan 头参数**（Qt6 WebEngine 需要，头已解到 `~/opt/vulkan-hdr`）：
```bash
cd /home/bit/workspace/charging-pile
cmake -S . -B build -DVulkan_INCLUDE_DIR=$HOME/opt/vulkan-hdr/usr/include
cmake --build build -j2
ctest --test-dir build --output-on-failure        # 3 套测试
```

## 2. 运行（演示路径）
```bash
build/src/backend_server/ncs_server /tmp/ncs.db 8080 18000   # 默认 db ./ncs-backend.db http8080 simTCP18000，第4参=预约超时秒
build/src/simulator/ncs_simulator 127.0.0.1 18000 2 1 9      # host port 间隔 起始桩 桩数
build/src/client_user/ncs_user                                # C 端；ssh -X 弹窗
```
C 端先登录(11 位手机号，验证码 123456)→ 找桩(我的位置距离/点站看桩)→ 点桩充电(预约/开始/实时/结束出账单/支付)→ 个人中心(充值/改昵称/历史/头像/我的会话)。
浏览器导航需外网(Leaflet/OSM)；`uploads/` 存头像(已 gitignore)。

## 3. 统一约定（新增任何端都必须遵守）
- **金额**：一律 `ncs::MoneyCents`=qint64"分"；显示 `format_cents()`；计费 half-up 到分(`charging_amount_cents`)。
- **API 信封**：`{code,message,data}`；code 0 成功 / 1 业务失败 / 2 参数错(HTTP 400)；列表在 `data` 数组。
- **客户端服务**：一律**异步回调**接口（`I*Service` + `Http*Service`/`Mock*Service`），禁止同步阻塞(QEventLoop)——充电页实时刷新依赖它。
- **实体建模**：只放"需持久化/展示且不能现算"字段；在线率/距离/累计次数等不入实体。
- **页面**：单例 + 返回/进入时主动 `refresh()/load()`（否则状态不刷新）。
- **WebEngine**：只链入可执行(ncs_user)，不链库/测试；新地图类页面照 `NavigationPage`(MainWindow::pushPage/popPage 动态装配)。
- **命名**：目录/目标/命名空间 `ncs`；控件用稳定 objectName（便于无头测试与迁移前端壳）。

## 4. HTTP 接口（后端，信封统一）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /health | 服务信息 |
| POST | /api/auth/send-code {phone} | 验证码(演示 123456) |
| POST | /api/auth/login {phone,code} | 免密登录/自动注册 |
| GET | /api/stations | 站列表(带 price_cents/free_piles) |
| GET | /api/stations/{id}/devices | 站内桩列表 |
| POST | /api/orders {phone,device_id} | 预约占桩(单价快照)；**有未支付账单则拒绝** |
| POST | /api/orders/{id}/start \| /finish \| /pay \| /cancel | 开始 / 结束出账单(不扣钱) / 支付扣款 / 取消(仅预约) |
| GET | /api/orders/{id}/live | 实时 energy/amount(充电页每秒轮询) |
| GET | /api/orders/active?phone= | 我的活跃会话(Reserved/Charging/Completed) |
| GET | /api/orders/history?phone=&limit=&offset= | 已支付历史(翻页, limit≤100) |
| POST | /api/wallet/recharge {phone,amount_cents} | 模拟充值 |
| PATCH | /api/user/profile {phone,nickname} | 改昵称(**PATCH**，客户端走 patch()) |
| POST | /api/user/avatar?phone=&ext=png | 头像上传(raw PNG body, ≤2MB) |
| GET | /uploads/avatar_<phone>.png | 头像静态文件 |

订单状态：`Reserved=0 预约 / Charging=1 / Completed=2 待支付 / Paid=3 / Canceled=4`。
桩状态：`Idle=0 / Charging=1 / Fault=2 / Reserved=3`。
**语义**：结束≠扣款；`pay` 才扣(允许欠费)；未支付不能开新桩；预约超时由后端线程自动释放。

## 5. 模拟器协议（TCP JSON-lines）
- 连上先发 `{"type":"register","devices":[ids...]}`。
- 后端可下发 `{"cmd":"start|stop","device_id":N}`；模拟器据此切换桩状态，充电中每心跳累加 `energy_kwh` 上报。
- 心跳：`{"type":"heartbeat","device_id":..,"voltage":..,"current":..,"temperature":..,"power_kw":..,"energy_kwh":..,"charging":bool,"ts":..}`
- 后端 `IHeartbeatSink`(现 LogSink) 是未来 AI/入库的处理点。

## 6. 后端并发与一致性
- SQLite C API(serialized) + `Store` 互斥；跨多步业务(预约/结算/支付/取消/清扫)在 **`beginTx/commit/rollback` 事务**里；同线程事务内方法用 `lockGuard` 不重复加锁。
- `ChargeService` 自持一把锁防业务交错。
- 已知 demo 限制：无 token/会话(写操作按 phone 自报)；历史分页非原子(翻页瞬间 Paid 增量可能跳行)。

## 7. 测试
- `ctest` 三套：offscreen(Qt 界面/服务)/ backend(Store+并发+事务+HTTP+sim+充电/取消/超时/钱包/历史/头像)/ e2e(真闭环)。
- WebEngine 不链入测试库，无头不会因 GLX/沙箱崩。curl 冒烟可能被权限分类器拦，用 ctest 代替。

## 8. 环境备注（132）
- Node/其他与本仓库无关。构建 WebEngine 曾缺：`qt6-webengine-dev-tools`、Vulkan 头(已装/解包)，勿删 `~/opt/vulkan-hdr`。
- 每次新成员上手：先 `git status`(可能有并行窗口未提交)、读 `docs/ncs-srs.md`、`notes/`(每切片决策)。

### 管理端(B1)
- 运行：`build/src/client_admin/ncs_admin`(ssh -X，1280×800)。账号 `admin` / `admin123`。
- 共享 HttpJsonClient 已提升到 `src/shared`(ncs_shared)，C 端与 admin 共用一份。
- 资产操作为：建站(名/址/经纬/单价)→ 批量建桩(数/型/功率) → 删桩/删站(站下有桩禁删)。
