# NCS 充电桩 O2O 平台 · 使用说明

> 北理工课设。多端：**C 车主端(手机)** / **B 运营管理端(桌面)** / **Web 决策大屏** / **后端** / **模拟器**。
> 详细需求：`docs/ncs-srs.md`；接口对照表：`docs/汇报材料-功能架构接口.md`；切片决策与验收：`notes/`。
> 本文只讲四件事：①这平台是什么；②新机器要装什么；③怎么编译；④怎么跑起来。

---

## 1. 基本介绍

一套"充电桩 + 平台"的最小闭环演示：**物理桩(模拟器) → 后端(HTTP+TCP 网关) → C/B/Web 三个前端**。

核心关系一句话：**后端 DB 是唯一真源**（桩台账、站点、功率、单价、订单、余额都存在 SQLite 里）；模拟器扮演"真实桩"，连上后端后服从后端下发命令（开始/停止/远程重启），并周期性心跳上报功率/电量/故障；前端一律走后端 REST，不直接碰模拟器。

各端角色：

| 端 | 可执行/目录 | 干什么 | 依赖 |
|---|---|---|---|
| C 车主端 | `ncs_user`（`src/client_user`） | 登录→找站/地图→选桩→预约→开始充电→结束→支付→个人中心 | Qt6 Widgets + core |
| B 运营端 | `ncs_admin`（`src/client_admin`） | 营收分析/桩态/桩与站管理/用户管理/预测；RBAC 三角色 | Qt6 Widgets+Charts + core |
| 后端 | `ncs_server`（`src/backend_server`） | HTTP(8080) + 模拟器 TCP(18000) + SQLite | Qt6 Core + 系统 sqlite3 |
| 模拟器 | `ncs_simulator`（`src/simulator`） | 纯 C++ 无 Qt，模拟 9 台桩上报/受控 | 无（纯标准库） |
| Web 大屏 | `web/`（Vue3+ECharts+Vite） | 决策者数据大屏，走 `/api` 代理取数 | node ≥18（仅此端需要） |
| 前端数据层 | `ncs_front_core`（`src/core`） | C/B 共用，同步走后端 REST | Qt6 Core/Gui/Network |

- 默认账号：管理员 `admin/admin123`(super) · `operator/operator123` · `viewer/viewer123`；C 端演示验证码固定 `123456`。
- 默认桩台账：3 站 9 台（望京站桩 1–3 / 中关村站桩 4–7 / 亦庄站桩 8–9），与模拟器 `1..9` 一一对应。

关键口径速记（详见 docs）：
- 金额一律"分"(qint64)；API 信封 `{code,message,data}`，0=成功 1=业务失败 2=参数错 3=未认证 4=无权限。
- 订单态 `0 预约 / 1 充电中 / 2 待支付 / 3 已支付 / 4 已取消`；桩态 `0 空闲 / 1 充电中 / 2 故障 / 3 预约中 / 4 重启中`。
- 余额不为负；有待支付账单不能开新单；同一用户可多桩并发。
- 后端按功率分档计费，`unit_price_cents` 预约时快照。

---

## 2. 配环境（新机器克隆后）

前置（假设都有，不再列命令）：**Ubuntu 22.04**、`g++`/`make`、`cmake ≥ 3.16`、**Qt6**（本项目在 6.2.4 上验证；`qt6-base-dev` 提供 Core/Gui/Widgets/Network/Test）。

### 2.1 必装（本项目真正"多出来"的）

后端用 SQLite C API 编译期链接系统 `sqlite3`，B 端图表用 Qt6 Charts（顶层 CMake `REQUIRED`，缺了整个工程配置不过）：

```bash
sudo apt update
sudo apt install -y libsqlite3-dev          # ncs_server 链接 sqlite3
sudo apt install -y libqt6charts6-dev       # ncs_admin 图表 / 顶层 find_package(Charts)
sudo apt install -y libvulkan-dev   # WebEngine(预留)编译需 Vulkan 头，默认装到 /usr/include/vulkan
```

### 2.2 预留（现在编译用不到，将来 C 端嵌地图时启用）

C 端地图将来要换成 `QWebEngineView` 嵌入网页地图，先把 Qt6 WebEngine 装上（apt 装的版本无需额外 Vulkan 头；只有非 apt 方式装的 Qt 才需要 `-DVulkan_INCLUDE_DIR`）：

```bash
sudo apt install -y qt6-webengine-dev libqt6webenginecore6-bin
```

> 备注：当前代码**没有**用 WebEngine，装不装都不影响本次编译运行；将来接入时还需在顶层 CMake 的 `find_package` 补 `WebEngineWidgets`、`ncs_user` 链接 `Qt6::WebEngineWidgets`，并在运行时确认有 `WebEngineProcess`（即上面 `-bin` 包）。

### 2.3 Web 大屏（仅前端需要 node ≥18）

Ubuntu 22.04 apt 自带的 `nodejs` 太老（Vite 跑不动）。推荐 NodeSource 装 Node 20，或用已有的 `nvm`/自定义安装（本机 132 用的是 `/opt/node`，已入 PATH）：

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
node -v   # ≥ 18 即可
```

### 2.4 快速自检

```bash
pkg-config --modversion Qt6Core 2>/dev/null || dpkg -l | grep qt6-base-dev
dpkg -l | grep -E "libsqlite3-dev|libqt6charts6-dev"   # 两条都应在
```

---

## 3. 构建

```bash
cmake -S . -B build              # 默认即可：Vulkan 头在标准位置 /usr/include/vulkan 时自动探测
# 仅当 Vulkan 头在非默认位置(如自编译 Qt 用的 ~/opt/vulkan-hdr)才追加：
#   cmake -S . -B build -DVulkan_INCLUDE_DIR=$HOME/opt/vulkan-hdr/usr/include
cmake --build build -j4
ctest --test-dir build --output-on-failure    # 2 套测试（ncs_offscreen + ncs_backend）
```

Web 大屏（可选，跑网页端才需要）：
```bash
cd web && npm install && npm run build    # 产物 web/dist/（单文件，可 file:// 打开）
```

产物路径：`build/src/backend_server/ncs_server`、`build/src/simulator/ncs_simulator`、`build/src/client_user/ncs_user`、`build/src/client_admin/ncs_admin`。

首次构建常见坑：
- 缺 `data/` 目录 → 后端会建 DB 失败，先 `mkdir -p data`（见运行节）。
- `simulator` 是纯 C++，缺 Qt 也不该报 Qt 错；若报，多半是顶层 `find_package` 少装了 Charts（见 2.1）。
- 换机器后第一次跑会自动 seed 3 站 9 桩 + 三管理员，不用手动灌库。

---

## 4. 运行（演示动线）

先**后端**，再**模拟器**，之后随便起前端。

```bash
# ① 后端(常驻)：data/ncs.db 是持久库(已 gitignore)。库放 data/，勿放 /tmp——重启/清临时会丢账号与订单
mkdir -p data
build/src/backend_server/ncs_server data/ncs.db 8080 18000 900 5
#    参数：DB路径 HTTP端口 模拟器端口 预约超时秒 清扫周期秒

# ② 模拟器(常驻)：模拟 id 1..9 共 9 台桩
#    参数：host port 心跳间隔 startId count autoFaultSec(>0 周期性把首桩置故障，供"远程重启"演示)
build/src/simulator/ncs_simulator 127.0.0.1 18000 2 1 9 12
```

验证后端/桩已就绪：`curl http://127.0.0.1:8080/health`；模拟器日志出现 `registered 9 devices` 即正常。

```bash
# ③ C 端(B 手机端)与 B 端(桌面)，需要图形界面：
#    - 在 132 桌面直接开，或
#    - Windows Git Bash 用已配好的 X11 转发：ssh -X 192.168.150.132，再执行下面两行
build/src/client_user/ncs_user      # C：手机号登录(验证码 123456)→找站→选桩→预约→开始→结束→支付
build/src/client_admin/ncs_admin    # B：admin/admin123 等；含 标记故障→远程重启 演示
```

```bash
# ④ Web 决策大屏(另终端；先起后端，vite /api 代理到 8080)
cd web && npm run dev               # http://localhost:5173
```

运行参数说明：
- C/B 端默认连 `http://127.0.0.1:8080`，可用环境变量 `NCS_BACKEND_URL` 覆盖（如连远端后端）。
- **模拟器台数须与后端桩台账对齐**：后端 DB 里有多少台桩(默认 9)，`startId=1 count=9` 就要覆盖全 1..9。在 B 端"登记"真实桩是建 DB 记录——登记≠凭空多出设备，物理桩要先存在（演示里即模拟器要多管一台），再在管理端登记它，这与现实"先有桩后入台账"一致。
- 端到端最小动线：C 登录 → 找站 → 选空闲桩 → 预约 → 开始 → 观察 live 电量/金额涨 → 结束(出待支付账单) → 立即支付(余额扣减)。B 端可对故障桩走"标记故障→远程重启→自动恢复空闲"。

---

## 附：更多参考
- HTTP 接口表 / 计价 / 统计口径：`docs/汇报材料-功能架构接口.md`。
- 模拟器 TCP 协议细节：`docs/` 与 `notes/`；`ncs_simulator` 心跳含 `power_kw/energy_kwh/sim_state`。
- 已知限制 / 待办 / 各切片决策：`notes/`。
