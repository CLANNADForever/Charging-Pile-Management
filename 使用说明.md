# NCS 充电桩 O2O 平台 · 使用说明

> 北理工课设。多端：C 车主端 / B 运营管理端 / Web 决策大屏 / 后端 / 模拟器。
> 需求与产品：`docs/ncs-srs.md`；接口对照：`docs/汇报材料-功能架构接口.md`；各切片决策与验收：`notes/`。

## 1. 目录结构（整合后）
```
charging-pile/
├── CMakeLists.txt                 # 顶层：Qt6 Core/Widgets/Charts/Network + src/ + tests/
├── src/
│   ├── common/     共享：entities / money(分) / billing(half-up) / phone / soc / 分档价 / haversine
│   ├── shared/     HttpJsonClient(Qt 异步，admin 与旧链路共用)
│   ├── backend_server/  后端库 ncs_server_lib + 可执行 ncs_server(HTTP+模拟器TCP+SQLite)
│   ├── simulator/   模拟器 ncs_simulator(纯 C++)
│   ├── core/        新前端数据层 ncs_front_core：service(同步走后端 REST) + net/BackendClient
│   ├── client_user/ 新 C 端 UI → 可执行 ncs_user
│   └── client_admin/ 新 B 端 UI → 可执行 ncs_admin
├── web/           决策者数据大屏(Vue3+ECharts+Vite；只提交源码)
├── tests/         ctest：ncs_offscreen(新 core 冒烟) + ncs_backend
├── third_party/   vendored：httplib.h、nlohmann/json.hpp
└── docs/ notes/ 使用说明.md
```
> 旧 `src/client_user|admin`(偏薄旧版)已退役删除；`other/`(临时交接目录)已清理。新 UI 数据层一律走后端。

## 2. 构建（132 VM）
Qt6 6.2.4(Widgets/Network/WebEngine/Charts)、系统 SQLite、third_party vendored。WebEngine 需 Vulkan 头：
```bash
cmake -S . -B build -DVulkan_INCLUDE_DIR=$HOME/opt/vulkan-hdr/usr/include
cmake --build build -j4
ctest --test-dir build --output-on-failure        # 2 套
```
Web 大屏需 node≥18：132 已装 Node20(`/opt/node`，已入 `~/.profile`)：
```bash
cd web && npm install && npm run build   # 产物 web/dist/
```

## 3. 运行（演示）
```bash
# 1) 后端(常驻)：http8080  simTCP18000  预约超时900s  重启自动恢复5s
build/src/backend_server/ncs_server /tmp/ncs.db 8080 18000 900 5
# 2) 模拟器(演示 9 台桩；第6参>0 周期性把首桩置故障，供远程重启演示)
build/src/simulator/ncs_simulator 127.0.0.1 18000 2 1 9 12
# 3) C/B 端(ssh -X)
build/src/client_user/ncs_user        # C：登录→找站→选桩→预约→开始→结束→支付→个人中心
build/src/client_admin/ncs_admin      # B：admin/admin123(super) 等
# 4) Web 大屏(另终端；vite /api 代理到 8080)
cd web && npm run dev                 # http://localhost:5173
```
C 端数据层默认走后端(`NCS_BACKEND_URL` 可覆盖，默认 `127.0.0.1:8080`)；登录走 **send-code+login**，演示验证码 **123456**。

## 4. 统一约定
- **金额**：一律 `ncs::MoneyCents`=qint64「分」；计费 half-up 到分。前端 Service 层 ×/÷100 换算成元。
- **API 信封**：`{code,message,data}`：0 成功 / 1 业务失败 / 2 参数错(HTTP400) / 3 未认证(401) / 4 无权限(403)(仅 B 端)。
- 时间一律 ISO(UTC)。C 端按手机号自报(无 token)；B 端 `/api/admin/*` 登录后带 `Authorization: Bearer <token>`。
- **余额永不为负**：支付不足拒付并提示"余额不足请先充值"；负余额用户不能约桩。
- **多桩并发**：有「正在充电」不挡新开；存在**待支付(未结算)账单**才禁止开新订单。
- 前端分两层：**旧链路**(HttpJsonClient 异步回调) vs **新前端 core**(同步走后端)；UI 只读/写 Service 层，网络不直接进 View。

## 5. HTTP 接口（后端，信封统一）
### C 端（公开）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /health | 服务信息(已加 CORS) |
| POST | /api/auth/send-code {phone} | 下发验证码(演示 123456，提示含码) |
| POST | /api/auth/login {phone,code} | 免密登录/自动注册；冻结账号拒绝 |
| GET | /api/stations | 无参=全量数组(兼容)；带参=`{items,total,page,page_size}` 富查询 |
| GET | /api/stations/hot?limit= | 近 7 日已支付单数 TopN |
| GET | /api/stations/{id}/devices | 站内桩(状态/功率/电量) |
| POST | /api/orders {phone,device_id} | 预约(占桩、按功率档快照单价、返回 expires_at)；需余额≥站起充 |
| POST | /api/orders/{id}/start \| /finish \| /pay \| /cancel | start 返回更新后订单；finish 只出**待支付**账单(不扣款)；pay 才扣款(不足拒付)；cancel 仅预约 |
| GET | /api/orders/{id}/live | 实时 energy/amount/power_kw/soc_pct/elapsed_sec |
| GET | /api/orders/{id} | 订单详情/小票(全字段+站名+功率档+时长+SoC+当前余额) |
| GET | /api/orders/active?phone= | 我的活跃会话(预约/充电/待支付) |
| GET | /api/orders/history?phone=&limit=&offset= | 已支付历史(分页) |
| POST | /api/wallet/recharge {phone,amount_cents} | 模拟充值 |
| PATCH | /api/user/profile {phone,nickname} | 改昵称(**PATCH**) |
| POST | /api/user/avatar?phone=&ext=png | 头像上传(raw PNG ≤2MB，白名单+魔数) |
| GET | /uploads/avatar_<phone>.png | 头像静态 |

### 站富查询参数(`/api/stations`)
`q`(名/址关键字) · `amenities`(位掩码，需全含) · `power_tier=slow|fast|ultra` · `parking` · `location` · `is_promo` · `radiusKm`(配合 `lat/lng`) · `sort=distance|price|recommend` · `page/page_size`。

### B 端 /api/admin/*（RBAC）
- 角色：`super` 全部；`operator` 只读+远程重启/故障/恢复；`viewer` 只读。种子：`admin/admin123` · `operator/operator123` · `viewer/viewer123`。
- 敏感动作(登录/冻结/建改删站桩/重启/故障恢复)自动写 `admin_audit`；设备故障/重启/恢复写 `device_ops`。

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/admin/login | 登录签发 token(返回 role/token) |
| GET | /api/admin/users?phone=&status= | 用户(0 全部/1 正常/2 冻结) |
| POST | /api/admin/users/{id}/freeze {frozen} | 冻结/解冻(super) |
| GET | /api/admin/stations?q= | 站+在线率 |
| POST/PATCH/DELETE | /api/admin/stations[/{id}] | 建/改/删站(有桩禁删)；可传 amenities/parking/location/is_promo/open_hours/min_charge_cents/price_slow/price_ultra_cents |
| POST | /api/admin/stations/{id}/devices {count,type,power_kw} | 批量建桩(站不存在拒绝，整批事务回滚) |
| GET | /api/admin/devices?q=&station_id=&type=&state=&page=&page_size= | 设备列表(带站名/在线/功率/电量/累计次数时长) |
| DELETE | /api/admin/devices/{id} | 删桩(非空闲拒绝) |
| POST | /api/admin/devices/{id}/restart | 远程重启(仅 Fault；super/operator；进入重启中→自动恢复) |
| POST | /api/admin/devices/{id}/fault {on} | 运维：空闲→标记故障 / 故障·重启中→恢复(super/operator) |
| GET | /api/admin/logs/ops \| /logs/audit?page=&page_size= | 运维/审计日志 |
| GET | /api/admin/stats/overview | 今日/本月/累计 营收·订单·电量 + 设备健康度 + 各站在线率 |
| GET | /api/admin/stats/daily?days= | 近 N 日按日营收/订单/电量(缺日补 0) |

### 状态枚举
- 订单：`Reserved=0 预约 / Charging=1 / Completed=2 待支付 / Paid=3 / Canceled=4`。
- 桩：`Idle=0 / Charging=1 / Fault=2 / Reserved=3 / Rebooting=4(重启中)`。
- 前端新 UI 展示口径：0 预约 /1 充电中 /2 已完成(含待支付与已支付) /3 已取消。

### 计价/统计口径
- 站按功率档计价：快充档=`price_cents`(别名 `price_fast_cents`)、慢/超档 `price_slow/ultra_cents`；档位 `power_tier`(慢<30 / 快 30–180 / 超 ≥180)；未配置档回退快充档；`Order.unitPriceCents` 在预约时按**桩功率档**快照。
- SoC(标准电池)：`battery_cap_kwh=60` + `start_soc_pct=20` 快照，`soc = clamp(start + energy/cap×100, 0..100)`；时长以 `charge_started_at→finished_at` 计。
- 营收 = 已支付订单按 `finished_at`(UTC) 窗口聚合；在线率 = 已连接模拟器桩数/总数；累计次数/时长 = 按订单聚合。

## 6. 模拟器协议（TCP JSON-lines）
- 连上先 `{"type":"register","devices":[ids...]}`；后端可下发 `start|stop` 与 `restart`(仅故障桩，约 3s 自愈)。
- 心跳含 `power_kw / energy_kwh / charging / sim_state(-1/0/1/2/4) / ts`；`sim_state` 驱动设备"自主故障/恢复/重启"落库。
- CLI：`ncs_simulator host port interval startId count autoFaultSec`(autoFaultSec>0 周期性把起始桩置故障)。

## 7. Web 数据大屏（web/）
- 技术 Vue3+ECharts+Vite；布局固定，仅数据层(useDashboard)走后端。
- 启动：先起后端(admin/admin123)，再 `cd web && npm install && npm run dev`；vite `/api` 代理→8080。
- 取数：自动 admin 登录取 token → `/api/admin/stats/overview|daily`(KPI/近7日趋势/设备健康度)；其余面板保留静态；后端不可达自动回退静态。
- `npm run build` → `web/dist/index.html`(单文件，可 file:// 打开)。

## 8. 测试
- `ctest` 2 套：
  - `ncs_offscreen`：实体/money + 新前端 core 冒烟(进程内后端：登录/站桩读/充值/昵称/头像/模拟器充电闭环(预约-开始-心跳-结束-支付)/B 端 CRUD+故障+重启+删)。
  - `ncs_backend`：Store/事务/HTTP(含 admin RBAC/分档价/富查询/fault 运维)/模拟器/并发。
- Web 端未纳入 ctest(需 node)；离线回退静态保证可看。

## 9. 已知限制 / 待办（记 notes）
- 新 C/B UI 桩态只有 空闲/使用/故障 三态，后端 `Reserved/Rebooting` 折叠成"使用"（远程重启后用运维日志观察流转）。
- C 端头像/昵称/充值等写操作需后端在线；离线 UI 能开但读列表为空并提示。
- 新 UI 页面级自动化尚未纳入 ctest(仅 service 冒烟)。
- 大屏"各站营收/热力图/用户增长/负荷预测"等仍为静态占位。
- notes/ 含各切片决策与验收(如余额非负、start 返回订单、站内"我的活跃桩"、RBAC 等)。

## 10. 各切片进度速览（notes 可查）
R1–R11(实体/分档/功率口径/订单时刻/SoC/余额门禁/预约截止/live/详情/富查询/热门) → B1/B2(管理端 RBAC+风控+资产+监控+重启+审计+统计) → 新前端接入片1–片5(工程整合/只读/写闭环/收口/web 大屏) 均已完成。
