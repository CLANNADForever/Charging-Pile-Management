# 2026-09-04 · 第二片：手机号免密登录（MockService）+ 金额改"分"

## 本次做了什么
- **金额落地"分"**（你定的规则，已进代码）：
  - `src/common/money.h`：`using MoneyCents = qint64` + `format_cents()`（分→元字符串）
  - `User.balance`(double) 已删，改为 `User.balanceCents`（分）
- **C 端登录闭环（无后端可测）**：
  - `services/IUserService.h` —— 纯虚接口：`requestCode(phone)` / `login(phone, code)`
  - `services/MockUserService` —— 内存实现：11 位手机号校验（`^1[0-9]{10}$`）、验证码固定 `123456`、号码不存在自动注册（昵称"充电用户"+尾号4位）
  - `views/LoginPage` —— 手机号 + 验证码 + 两个按钮 + 提示
  - `views/ProfilePage` —— 个人中心占位（昵称/手机号/余额，余额用 format_cents）
  - `MainWindow` 改为中央 QStackedWidget 导航（0=登录 1=个人中心），构造注入 `IUserService*`
- 测试：QtTest offscreen **10 用例全过**（金额格式化/实体/手机号校验/错码提示/成功登录自动注册并切页/窗口尺寸）；`ncs_user` 冒烟正常。

## 常用命令
```bash
cmake -S . -B build && cmake --build build -j2
ctest --test-dir build --output-on-failure          # offscreen，自动设 QT_QPA_PLATFORM
build/src/client_user/ncs_user                      # 有 X11 时窗口会弹出
```

## 需要你过目 / 决定
1. [ ] **ssh -X 跑一次真实登录**：输入 `13800138000` → 获取验证码 → 提示里出现 123456 → 填 123456 → 登录 → 应切到"个人中心"，余额显示 `0.00 元`。
2. [ ] 确认登录流程/文案可接受（界面很朴素，只是逻辑示例——等你同学的美观前端壳）。
3. [ ] **接口接缝清单**（以后对接后端/迁移同学前端都靠它们对齐，别改名字）：
   - `services/IUserService.h`（HttpUserService 替换点）
   - `views/LoginPage` / `views/ProfilePage` 的 objectName：
     `phoneEdit codeEdit btnSendCode btnLogin loginStatus profileNickname profilePhone profileBalance`
   - 登录成功信号 `LoginPage::loginSucceeded(const ncs::User&)`

## 我的建议 · 下一片候选（你选）
A. **实体建模补齐**：Station / Order / 充电记录 + 计费规则（单价单位也是"分"）——建议先做，后续切片的数据才有依托。
B. **周边找桩页**（假数据）：站点列表 + 空余桩数 + 模拟定位。
C. **后端起步**（HTTP + SQLite + 同一套实体）：把 IUserService 换 HttpUserService。
（到了核心的**计费/充电状态机、并发防超卖**部分，我会先把技术方案选项摆给你再动手。）
